const routes = {
  getAgencyCommissions: '/api/agency-commissions',
  remindSupplier: '/api/agency-commissions/:bookingId/remind-supplier',
  remindClient: '/api/agency-commissions/:bookingId/remind-client',
}

export default routes
