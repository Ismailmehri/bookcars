const routes = {
  create: '/api/create-subscription',
  getCurrent: '/api/my-subscription',
  getSubscriptions: '/api/subscriptions/:page/:size',
  getSubscription: '/api/subscription/:id',
  update: '/api/update-subscription',
}

export default routes
