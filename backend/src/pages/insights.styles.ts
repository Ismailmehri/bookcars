import { type SxProps, type Theme } from '@mui/material/styles'

export const insightsPageContainerSx: SxProps<Theme> = {
  width: '100%',
  maxWidth: '100%',
  minWidth: 0,
  px: {
    xs: 2,
    md: 0,
  },
}
